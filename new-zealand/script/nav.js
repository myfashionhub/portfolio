function Navigation() {
    var that = this;

    this.init = function(numPages) {
        this.numPages = numPages;
        this.createNav();
    };

    this.createNav = function() {
        for (var i = 0; i < this.numPages; i++) {

        }
    };

    this.init();
}
